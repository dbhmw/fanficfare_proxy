services:
  fff_proxy:
    image: fff_proxy:latest
    build: .
    ports:
      - "23000:23000"
    volumes:
      - ./server_certs:/home/fff_proxy/server_certs:ro
    restart: on-failure
    tmpfs:
      - /tmp:rw,size=1g,mode=1777
